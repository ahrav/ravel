# bv (beads viewer) local config and caches
.bv/

# beads/bv per-machine runtime files (bd rewrites .beads/.gitignore, so pin here)
.beads/.bv.lock
.beads/daemon-error
.pi-subagents/

# Cargo build artifacts
/target
