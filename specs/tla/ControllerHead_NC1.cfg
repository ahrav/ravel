\* NC1: resolve()'s owner rule without its epoch conjunct (renew/acquire
\* conflation).  Expected: ResolutionSound violated (a false Proven).
\* Negative control -- this run MUST fail.
SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT EpochMonotonic
INVARIANT ResolutionSound
INVARIANT NoDualAuthority
INVARIANT FencingOrder

CONSTANT Controller = {c1, c2}
CONSTANT MaxInc = 2
CONSTANT MaxEpoch = 4
CONSTANT MaxTerm = 4
CONSTANT MaxTail = 1
CONSTANT MaxReq = 2
CONSTANT ResolveIgnoresEpoch = TRUE
CONSTANT BlindRetry = FALSE
CONSTANT FencingOff = FALSE
