\* NC2: blind CAS retry on an unknown outcome instead of resolving, over a head
\* that has since moved (`BlindRetryCas` is guarded on `RegB # ctl[c].cand`, so
\* the byte-identical -- and safe -- `RetryIdentically` rewrite is not modelled).
\* Expected: EpochMonotonic / NoDualAuthority violated (two DISTINCT authority
\* writes at one epoch: the retry overwrites a rival's committed, already proven
\* authority).
\* Negative control -- this run MUST fail.
SPECIFICATION Spec

\* Only the target property is listed so the reported violation IS that property.
INVARIANT TypeOK
INVARIANT EpochMonotonic

CONSTANT Controller = {c1, c2}
CONSTANT MaxInc = 2
CONSTANT MaxEpoch = 4
CONSTANT MaxTerm = 4
CONSTANT MaxTail = 1
CONSTANT MaxReq = 2
CONSTANT ResolveIgnoresEpoch = FALSE
CONSTANT BlindRetry = TRUE
CONSTANT FencingOff = FALSE
CONSTANT IgnoreIncarnation = FALSE
