\* NC3: fenced downstream write skips its ETag condition.
\* Expected: FencingOrder (S1) violated (a stale-epoch effect lands late).
\* Negative control -- this run MUST fail.
SPECIFICATION Spec

\* Only S1 is listed so the reported violation IS the fencing property.
\* (EpochMonotonic and NoDualAuthority also fail under this control; see README.)
INVARIANT TypeOK
INVARIANT FencingOrder

CONSTANT Controller = {c1, c2}
CONSTANT MaxInc = 2
CONSTANT MaxEpoch = 4
CONSTANT MaxTerm = 4
CONSTANT MaxTail = 1
CONSTANT MaxReq = 2
CONSTANT ResolveIgnoresEpoch = FALSE
CONSTANT BlindRetry = FALSE
CONSTANT FencingOff = TRUE
