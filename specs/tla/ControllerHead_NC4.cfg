\* NC4: incarnation-blind owner rule in resolve() -- what a reused, persisted
\* instance id would produce: a successor adopts its predecessor's term.
\* Expected: ResolutionSound violated.
\* Negative control -- this run MUST fail.
SPECIFICATION Spec

\* Only the target property is listed so the reported violation IS that property.
INVARIANT TypeOK
INVARIANT ResolutionSound

CONSTANT Controller = {c1, c2}
CONSTANT MaxInc = 2
CONSTANT MaxEpoch = 4
CONSTANT MaxTerm = 4
CONSTANT MaxTail = 1
CONSTANT MaxReq = 2
CONSTANT ResolveIgnoresEpoch = FALSE
CONSTANT BlindRetry = FALSE
CONSTANT FencingOff = FALSE
CONSTANT IgnoreIncarnation = TRUE
